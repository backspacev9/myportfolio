export interface Disk {
  id: number;
  label: string;
  image: string;
  rout: string;
}

export interface ProjectsItem {
  tittle: string;
  text: string;
  imgUrl: string | null;
  sourceUrl: string | null;
}
